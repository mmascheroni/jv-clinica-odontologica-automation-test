package pages;

import org.openqa.selenium.WebDriver;
import org.openqa.selenium.support.ui.WebDriverWait;

public class TurnoPage extends BasePage{

    public TurnoPage(WebDriver driver, WebDriverWait wait) {
        super(driver, wait);
    }
}
